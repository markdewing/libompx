
// Explicit instantiations for sort function
template void ompx_sort_by_key(int*, size_t, int*);
template void ompx_sort_by_key(int*, size_t, float*);
template void ompx_sort_by_key(int*, size_t, double*);
