
// Explicit instantiations for sort function
template void ompx_sort(int*, size_t);
template void ompx_sort(unsigned int*, size_t);
template void ompx_sort(float*, size_t);
template void ompx_sort(double*, size_t);
